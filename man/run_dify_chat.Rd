% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{run_dify_chat}
\alias{run_dify_chat}
\title{Send a chat request to a Dify Workflow via httr2 (BI-only)}
\usage{
run_dify_chat(
  instruction_prompt,
  api_key,
  api_upload = NULL,
  api_chat,
  user_id,
  file_path = NULL,
  detected_type = NULL,
  locally_parse_file = TRUE,
  conversation_id = NULL,
  deep_pdfscan = FALSE,
  force_parse = FALSE,
  model_name,
  debug = TRUE
)
}
\arguments{
\item{instruction_prompt}{Full prompt string to send as the query, including
any extracted file content if parsing locally}

\item{api_key}{Dify API key}

\item{api_upload}{API URL for the Dify file upload endpoint. Only used when
\code{locally_parse_file = FALSE}}

\item{api_chat}{API URL for the Dify chat/workflow endpoint}

\item{user_id}{User identifier string passed to the Dify API}

\item{file_path}{Path to the file on disk. Only used when
\code{locally_parse_file = FALSE}}

\item{detected_type}{MIME type of the file (e.g. \code{"application/pdf"}).
Only used when \code{locally_parse_file = FALSE}}

\item{locally_parse_file}{If \code{TRUE}, skips file upload and sends text
content directly in the prompt}

\item{conversation_id}{Default NULL, required for context reuse}

\item{deep_pdfscan}{Default FALSE, uses Vision to extract images}

\item{force_parse}{Default FALSE, forces reparsing}

\item{model_name}{Model name string used as a fallback label in usage_info
if the API does not return a model identifier}

\item{debug}{If \code{TRUE}, prints progress messages to the console}
}
\value{
A named list with the following elements:
\describe{
\item{answer}{Character string containing the raw model response}
\item{conversation_id}{Dify conversation ID string for the request}
\item{usage_info}{Named list with \code{input}, \code{output}, \code{total}
token counts and \code{model} label}
}
}
\description{
Handles the full Dify API interaction: optionally uploads a file first,
then sends the instruction prompt as a blocking workflow request and returns
the answer along with token usage metadata.
}
