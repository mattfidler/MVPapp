% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{run_ellmer_chat}
\alias{run_ellmer_chat}
\title{Send a chat request via an ellmer provider}
\usage{
run_ellmer_chat(
  service,
  model_name,
  system_prompt,
  optimal_params,
  api_chat = NULL,
  instruction_prompt,
  file_path = NULL,
  detected_type = NULL,
  locally_parse_file = TRUE,
  chat_obj = NULL,
  internal_version = TRUE,
  debug = TRUE
)
}
\arguments{
\item{service}{Provider to use. One of \code{"Gemini"}, \code{"OpenAI"},
\code{"Claude"}, \code{"OpenRouter"}, \code{"OpenAI-Compatible"}, \code{"DeepSeek"},
\code{"Apollo"}, \code{"Azure OpenAI"}, \code{"AWS Bedrock"}}

\item{model_name}{Model identifier string passed to the ellmer chat constructor}

\item{system_prompt}{System prompt string defining model behaviour and output
format constraints}

\item{optimal_params}{An \code{ellmer::params()} object controlling
temperature, seed, and other supported sampling parameters}

\item{api_chat}{Base URL for the chat endpoint. Only required when
\code{service = "openai_compatible"}}

\item{instruction_prompt}{Full user prompt string, including any extracted
file content if parsing locally}

\item{file_path}{Path to the file on disk. Only used when
\code{locally_parse_file = FALSE}}

\item{detected_type}{MIME type of the file (e.g. \code{"application/pdf"}).
Only used when \code{locally_parse_file = FALSE}}

\item{locally_parse_file}{If \code{TRUE}, sends text content in the prompt
directly rather than uploading the file to the provider}

\item{chat_obj}{the ellmer chat object for context re-use, default NULL}

\item{internal_version}{Logical. Changes base_url path, Only relevant for BI}

\item{debug}{If \code{TRUE}, prints progress messages and the chat object
summary to the console}
}
\value{
A named list with the following elements:
\describe{
\item{answer}{Character string containing the raw model response}
\item{chat_obj}{The ellmer chat object, which can be used for token
inspection or follow-up turns}
\item{usage_info}{Named list with \code{input}, \code{output}, \code{total}
token counts and \code{model} label}
}
}
\description{
Initialises the appropriate ellmer chat object for the given service and
dispatches the prompt, either as text-only or alongside an uploaded file.
Supports Gemini, OpenAI, Claude, OpenRouter, and OpenAI-compatible endpoints.
}
